---
id: package-cursor-rules
documentation_type: 'ai-package-documentation'
package_context:
    name: 'cursor-rules'
    type: 'tool'
    architecture_type: 'layered_library'
    main_exports: ['initCommand', 'upgradeCommand', 'replaceAllCommand', 'setGlobalRuleCommand', 'setMcpServerCommand', 'runCli']
    workspace_path: '.'
context7_refs: ['citty', 'picocolors', 'zod', 'typescript', 'vitest', 'gray-matter', 'micromatch']
module_docs:
    type: 'by_layer'
    rule: 'per_library'
    targets: ['src/cli/*', 'src/lib/*', 'src/model/*']
architecture_docs:
    format: 'single'
    root: 'architecture.xml'
---

# 📦 AI-документация: cursor-rules

<package_purpose>
**Назначение пакета:**
CLI инструмент для централизованного управления правилами `.cursor` в проектах. Обеспечивает инициализацию, обновление и замену конфигурационных файлов Cursor AI с отслеживанием версий.

**Решаемые задачи:**

- Единообразное распространение правил `.cursor` между проектами
- Версионное управление с инкрементальными обновлениями
- Автоматизация синхронизации правил при изменении базовой конфигурации
- Предотвращение конфликтов при обновлении существующих правил
</package_purpose>

<package_structure>
**Архитектурная схема:**

> **Примечание:** Детальная XML-структура пакета находится в файле `architecture.xml`. Данный файл является единственным источником правды об архитектуре.

**Тип архитектуры:** `layered_library`

**Основные слои:**

- **cli/** — Слой команд командной строки
- **lib/** — Слой утилит и библиотечных функций
- **model/** — Слой типов, констант и схем
</package_structure>

<key_features>
**Основные возможности:**

### Инициализация правил (init)

- Описание: Первичная установка правил `.cursor` в проект с созданием расширенной конфигурации
- Использование: `cursor-rules init`
- Особенности:
    - Проверяет наличие существующих правил, запрашивает подтверждение при конфликте
    - Создает файл конфигурации `.cursor/cursor-rules-config.json` с настройками по умолчанию
    - Поддерживает настройку языка вывода (`settings.language`)
    - Создает наборы правил (`ruleSets`) с возможностью управления обновлениями

### Обновление правил (upgrade)

- Описание: Инкрементальное обновление правил на основе diff между версиями с поддержкой гибкой конфигурации
- Использование: `cursor-rules upgrade`
- Особенности:
    - Читает `.cursor/cursor-rules-config.json`, вычисляет изменения (add/update/delete)
    - Применяет только необходимые изменения на основе `ruleSets` (обновляет только наборы с `update: true`)
    - Фильтрует файлы по `ignoreList` с поддержкой glob-паттернов (использует `micromatch`)
    - Применяет переопределения YAML frontmatter из `fileOverrides` (использует `gray-matter`)
    - Обновляет поле `updatedAt` в конфигурации

### Полная замена правил (replace-all)

- Описание: Замена всех правил на актуальную версию из пакета с сохранением конфигурации
- Использование: `cursor-rules replace-all`
- Особенности:
    - Удаляет старые правила полностью, копирует свежую версию
    - Сохраняет существующую конфигурацию `.cursor/cursor-rules-config.json` или создает новую при отсутствии
    - Применяет `ignoreList` и `fileOverrides` при копировании файлов
    - Обновляет версию и поле `updatedAt` в конфигурации

### Установка глобальных правил Cursor (set-global-rule)

- Описание: Добавление или обновление User Rules в настройках Cursor с интерактивным вводом параметров
- Использование: `cursor-rules set-global-rule`
- Особенности:
    - Всегда запрашивает переменные для подстановки в шаблон `meta-info.template.md` (даже если правило не существует)
    - Автоматическое определение существующих значений из текущего правила в настройках Cursor
    - Подстановка переменных (CURRENT_DATE, NAME, AGE, ROLE, STACK, TOOL_VERSIONS, OS, DEVICE, LOCATION, LANGUAGE, COMMUNICATION_STYLE)
    - Сохранение правила в настройках Cursor (`User/settings.json` в директории конфигурации Cursor)
    - Поддержка обновления существующих правил и добавления новых

### Управление User Rules через настройки (config user-rules)

- Описание: Интерактивное управление User Rules в настройках Cursor
- Использование: `cursor-rules config` → выбрать "User Rules"
- Особенности:
    - Просмотр списка всех User Rules
    - Добавление новых правил с интерактивным вводом имени и содержимого
    - Редактирование существующих правил
    - Удаление правил с подтверждением
    - Все изменения сохраняются в настройках Cursor (`User/settings.json`)

### Управление MCP серверами (set-mcp-server)

- Описание: Добавление или обновление конфигурации MCP серверов в Cursor
- Использование: `cursor-rules set-mcp-server`
- Особенности:
    - Интерактивный ввод параметров сервера (command, args, env)
    - Поддержка множественных переменных окружения
    - Автоматическое определение существующей конфигурации для обновления
    - Сохранение конфигурации в `mcp.json` в глобальной директории Cursor

### Расширенные возможности конфигурации

- **ruleSets**: Управление наборами правил с возможностью выборочного обновления и фиксации версий
- **ignoreList**: Исключение файлов из обновления с поддержкой glob-паттернов (`**/*.mdc`, `rules/custom-*.mdc`)
- **fileOverrides**: Переопределение YAML frontmatter для конкретных файлов (merge-стратегия)
- **settings.language**: Настройка языка вывода сообщений (`ru` или `en`)
</key_features>

<architecture_overview>
**Верхнеуровневая архитектура:**

- **cli/commands/*** — Модули команд (init, upgrade, replace-all, set-global-rule, set-mcp-server) с поддержкой нового формата конфигурации
- **cli/main** — Основной CLI entry point с Citty, автоматическая проверка версии перед выполнением команд
- **lib/file-operations** — Операции с файловой системой (node:fs/promises) с поддержкой `ignoreList` и `fileOverrides`
- **lib/version-manager** — Управление версиями, сравнение и автоматическое обновление пакета из npm registry
- **lib/diff-calculator** — Вычисление diff между версиями (add/update/delete)
- **lib/cursor-config** — Определение путей к конфигурации Cursor на разных ОС (включая путь к `User/settings.json`)
- **lib/cursor-rules** — Работа с User Rules в настройках Cursor и подстановка переменных в шаблоны
- **lib/mcp-config** — Чтение и запись конфигурации MCP серверов
- **lib/helpers** — Вспомогательные утилиты (проверка пустых строк и т.д.)
- **model/*** — Типы, константы, Zod схемы для конфигурации правил, MCP серверов и переменных шаблонов

**Взаимодействие модулей:**
Команды (cli/commands) используют утилиты (lib) для работы с файлами и версиями. Model слой предоставляет типы и схемы валидации для всех остальных модулей. CLI main автоматически проверяет версию пакета перед выполнением команд через version-manager.

**Особенности архитектуры:**

- Автоматическая проверка версии: перед выполнением любой команды `runCli` вызывает `ensureLatestVersion` для проверки обновлений в npm registry
- Расширенная конфигурация: все команды работают с новым форматом `.cursor/cursor-rules-config.json` вместо старого `.cursor-rules-version.json`
- Гибкое управление правилами: поддержка `ruleSets` для выборочного обновления, `ignoreList` для исключения файлов, `fileOverrides` для переопределения YAML параметров
</architecture_overview>

<detailed_modules>
**Детальная структура модулей:**

### Модуль: CLI Commands Init

**Статус:** ✅ Реализован
**Расположение:** `src/cli/commands/init/`
**Экспорты:** initCommand
**Файлы:**

- `index.ts` — Команда инициализации правил в проекте с созданием конфигурации `cursor-rules-config.json`
- `__tests__/init.test.ts` — Unit тесты

### Модуль: CLI Commands Update

**Статус:** ✅ Реализован
**Расположение:** `src/cli/commands/update/`
**Экспорты:** updateCommand
**Файлы:**

- `index.ts` — Команда обновления правил на основе diff с поддержкой `ruleSets`, `ignoreList` и `fileOverrides`
- `__tests__/update.test.ts` — Unit тесты

### Модуль: CLI Commands Replace-All

**Статус:** ✅ Реализован
**Расположение:** `src/cli/commands/replace-all/`
**Экспорты:** replaceAllCommand
**Файлы:**

- `index.ts` — Команда полной замены правил с сохранением существующей конфигурации
- `__tests__/replace-all.test.ts` — Unit тесты

### Модуль: CLI Main

**Статус:** ✅ Реализован
**Расположение:** `src/cli/main/`
**Экспорты:** runCli
**Внутренние функции:**

- `ensureLatestVersion` — Автоматическая проверка и обновление пакета до последней версии из npm registry
- `getPackageDir` — Определение директории пакета на основе пути к текущему файлу (поддержка production/development)
- `getTargetDir` — Получение текущей рабочей директории (process.cwd)
**Файлы:**

- `index.ts` — Основной CLI entry point с Citty, автоматическая проверка версии перед выполнением команд
- `ensure-latest-version.ts` — Проверка и обновление версии пакета
- `get-package-dir.ts` — Определение директории пакета
- `get-target-dir.ts` — Получение целевой директории
- `__tests__/main.test.ts`, `__tests__/get-package-dir.test.ts` — Unit тесты

### Модуль: File Operations

**Статус:** ✅ Реализован
**Расположение:** `src/lib/file-operations/`
**Экспорты:** copyRulesToTarget, deleteRulesFromTarget, readConfigFile, writeConfigFile
**Внутренние функции:**

- `applyYamlOverrides` — Применение переопределений YAML frontmatter к файлам правил (использует gray-matter)
- `shouldIgnoreFile` — Проверка файлов по списку игнорирования с поддержкой glob-паттернов (использует micromatch)
- `pathExists` — Проверка существования файла или директории
**Файлы:**

- `index.ts` — Фасад модуля с экспортами операций с файловой системой
- `copy-rules-to-target.ts` — Копирование правил с поддержкой `ignoreList` и `fileOverrides`
- `delete-rules-from-target.ts` — Удаление правил из целевой директории
- `read-config-file.ts` — Чтение конфигурации `cursor-rules-config.json`
- `write-config-file.ts` — Запись конфигурации `cursor-rules-config.json`
- `apply-yaml-overrides.ts` — Применение переопределений YAML frontmatter
- `should-ignore-file.ts` — Проверка файлов по списку игнорирования
- `path-exists.ts` — Проверка существования пути
- `__tests__/*.test.ts` — Unit тесты для всех функций

### Модуль: Version Manager

**Статус:** ✅ Реализован
**Расположение:** `src/lib/version-manager/`
**Экспорты:** checkAndUpdatePackage, compareVersions, getCurrentVersion, getNpmVersion, getPackageVersion, respawnProcess, updatePackage
**Файлы:**

- `index.ts` — Фасад модуля с экспортами управления версиями
- `check-and-update.ts` — Проверка и автоматическое обновление пакета с respawn механизмом
- `compare-versions.ts` — Сравнение версий (semver)
- `get-current-version.ts` — Получение текущей версии из конфигурации
- `get-npm-version.ts` — Получение последней версии из npm registry
- `get-package-version.ts` — Получение версии из package.json
- `update-package.ts` — Обновление пакета через npm/yarn
- `respawn-process.ts` — Перезапуск CLI процесса для загрузки новой версии кода
- `__tests__/*.test.ts` — Unit тесты (включая respawn-process.test.ts)

### Модуль: Diff Calculator

**Статус:** ✅ Реализован
**Расположение:** `src/lib/diff-calculator/`
**Экспорты:** calculateDiff, getFileHash, scanDirectory
**Файлы:**

- `index.ts` — Фасад модуля с экспортами вычисления diff
- `calculate-diff.ts` — Вычисление diff между версиями (add/update/delete)
- `get-file-hash.ts` — Вычисление хеша файла для сравнения
- `scan-directory.ts` — Рекурсивное сканирование директории
- `__tests__/*.test.ts` — Unit тесты

### Модуль: Model Types

**Статус:** ✅ Реализован
**Расположение:** `src/model/types/`
**Экспорты:** CommandType, RulesConfig, RuleSet, FileOverride, VersionComparison, VersionDiff, FileHashMap, CheckAndUpdateOptions
**Файлы:**

- `main.ts` — TypeScript типы для конфигурации правил, команд и версионирования (включая CheckAndUpdateOptions с полем isRespawn, TemplateVariables, McpConfig, McpServerConfig, GlobalRuleParams)

### Модуль: Model Constants

**Статус:** ✅ Реализован
**Расположение:** `src/model/constants/`
**Экспорты:** VERSION_FILE_NAME, RULES_DIRS, NPM_REGISTRY_URL, NPM_REQUEST_TIMEOUT
**Файлы:**

- `main.ts` — Константы проекта (имя файла конфигурации, директории правил, настройки npm registry)

### Модуль: Model Schemas

**Статус:** ✅ Реализован
**Расположение:** `src/model/schemas/`
**Экспорты:** rulesConfigSchema, ruleSetSchema, fileOverrideSchema, templateVariablesSchema, mcpServerConfigSchema, mcpConfigSchema, globalRuleParamsSchema
**Файлы:**

- `main.ts` — Zod схемы валидации конфигурации правил, переменных шаблонов и MCP конфигурации
- `command-params.ts` — Zod схемы валидации параметров команд
- `__tests__/schemas.test.ts` — Unit тесты

### Модуль: Cursor Config

**Статус:** ✅ Реализован
**Расположение:** `src/lib/cursor-config/`
**Экспорты:** getCursorConfigDir, getCursorRulesDir, getCursorMcpConfigPath, getCursorUserSettingsPath
**Файлы:**

- `index.ts` — Фасад модуля с экспортами определения путей конфигурации Cursor
- `get-cursor-config-dir.ts` — Определение базовой директории конфигурации Cursor на разных ОС (Windows, macOS, Linux)
- `get-cursor-rules-dir.ts` — Путь к директории глобальных правил Cursor (legacy)
- `get-cursor-mcp-config-path.ts` — Путь к файлу конфигурации MCP серверов
- `get-cursor-user-settings-path.ts` — Путь к файлу настроек пользователя Cursor (`User/settings.json`)
- `__tests__/*.test.ts` — Unit тесты для всех функций

### Модуль: Cursor Rules

**Статус:** ✅ Реализован
**Расположение:** `src/lib/cursor-rules/`
**Экспорты:** getMetaInfoTemplatePath, readGlobalRule, readUserRules, substituteTemplateVars, writeGlobalRule, writeUserRules
**Файлы:**

- `index.ts` — Фасад модуля с экспортами работы с глобальными правилами и User Rules
- `get-meta-info-template-path.ts` — Получение пути к шаблону meta-info.template.md
- `read-global-rule.ts` — Чтение файла глобального правила Cursor (legacy, для обратной совместимости)
- `read-user-rules.ts` — Чтение User Rules из настроек Cursor (`User/settings.json`)
- `write-global-rule.ts` — Запись файла глобального правила Cursor (legacy, для обратной совместимости)
- `write-user-rules.ts` — Запись User Rules в настройки Cursor (`User/settings.json`)
- `substitute-template-vars.ts` — Подстановка переменных в шаблон meta-info.template.md
- `__tests__/*.test.ts` — Unit тесты для всех функций

### Модуль: MCP Config

**Статус:** ✅ Реализован
**Расположение:** `src/lib/mcp-config/`
**Экспорты:** readMcpConfig, writeMcpConfig
**Файлы:**

- `index.ts` — Фасад модуля с экспортами работы с конфигурацией MCP серверов
- `read-mcp-config.ts` — Чтение и парсинг конфигурации MCP серверов из mcp.json
- `write-mcp-config.ts` — Запись конфигурации MCP серверов в mcp.json
- `__tests__/*.test.ts` — Unit тесты для всех функций

### Модуль: CLI Commands Set-Global-Rule

**Статус:** ✅ Реализован
**Расположение:** `src/cli/commands/set-global-rule/`
**Экспорты:** setGlobalRuleCommand
**Внутренние функции:**

- `extractVariablesFromContent` — Извлечение значений переменных из существующего правила
- `promptVariables` — Интерактивный запрос значений переменных для подстановки в шаблон
**Файлы:**

- `index.ts` — Команда установки User Rules в настройках Cursor с интерактивным вводом (всегда запрашивает переменные, даже если правило не существует)
- `extract-variables-from-content.ts` — Извлечение переменных из существующего правила
- `prompt-variables.ts` — Интерактивный запрос значений переменных
- `__tests__/set-global-rule.test.ts` — Unit тесты для команды

### Модуль: CLI Commands Config User-Rules

**Статус:** ✅ Реализован
**Расположение:** `src/cli/commands/config/`
**Экспорты:** manageUserRulesCommand (внутренняя функция)
**Внутренние функции:**

- `listRules` — Вывод списка всех User Rules
- `addRule` — Добавление нового правила
- `editRule` — Редактирование существующего правила
- `deleteRule` — Удаление правила с подтверждением
**Файлы:**

- `user-rules.ts` — Интерактивное управление User Rules (просмотр, добавление, редактирование, удаление)
- `index.ts` — Команда настройки с выбором действия (язык интерфейса или User Rules)

### Модуль: CLI Commands Set-MCP-Server

**Статус:** ✅ Реализован
**Расположение:** `src/cli/commands/set-mcp-server/`
**Экспорты:** setMcpServerCommand
**Внутренние функции:**

- `promptEnvironmentVariables` — Интерактивный запрос переменных окружения для MCP сервера
- `parseArgs` — Парсинг аргументов команды из строки (разделение по запятой)
**Файлы:**

- `index.ts` — Команда установки MCP сервера с интерактивным вводом параметров
- `__tests__/set-mcp-server.test.ts` — Unit тесты (если применимо)
</detailed_modules>

<e2e_testing>
**E2E тестирование:**

### Сценарий: Полный цикл init → update → replace-all

- **Файл:** `__tests__/e2e/full-cycle.e2e.test.ts`
- **Покрытие:** Инициализация в чистом проекте, обновление с версией diff, полная замена с новым форматом конфигурации
- **Статус:** ✅ Реализован

### Сценарий: Инициализация правил

- **Файл:** `__tests__/e2e/init-command.e2e.test.ts`
- **Покрытие:** Создание конфигурации `cursor-rules-config.json`, копирование правил, обработка конфликтов
- **Статус:** ✅ Реализован

### Сценарий: Обновление правил

- **Файл:** `__tests__/e2e/update-command.e2e.test.ts`
- **Покрытие:** Инкрементальное обновление с поддержкой `ruleSets`, `ignoreList` и `fileOverrides`
- **Статус:** ✅ Реализован

### Сценарий: Полная замена правил

- **Файл:** `__tests__/e2e/replace-all-command.e2e.test.ts`
- **Покрытие:** Полная замена правил с сохранением конфигурации, применение `ignoreList` и `fileOverrides`
- **Статус:** ✅ Реализован

**Вспомогательные утилиты для E2E тестов:**

- `helpers/copy-rules-fixtures.ts` — Копирование фикстур правил для тестирования
- `helpers/create-version-file.ts` — Создание файла конфигурации для тестов
- `helpers/temp-dir.ts` — Работа с временными директориями
</e2e_testing>

<technologies_used>
**Используемые технологии:**

- **TypeScript**: ^5.9.3 — Строгая типизация, ESM модули, target ES2022
- **Node.js**: >=18.0.0 — Современные API (fs promises нативный)
- **citty**: ^0.1.6 — Современный CLI builder (UnJS ecosystem, типобезопасный, async commands)
- **picocolors**: ^1.1.0 — Легковесная библиотека для цветного вывода (2x быстрее chalk, 14x легче, Trust Score 9.7)
- **zod**: ^4.1.12 — Валидация схем данных
- **gray-matter**: ^4.0.3 — Парсинг YAML frontmatter в файлах правил для применения переопределений
- **micromatch**: ^4.0.8 — Работа с glob-паттернами для фильтрации файлов по списку игнорирования

**Инструменты разработки:**

- vitest: ^4.0.6 — Тестирование с мокированием
- tsup: ^8.5.0 — Сборщик TypeScript
- eslint-walrus-config: ^1.1.0 — Линтинг кода
- ai-friendly-runner: ^0.4.2 — Параллельный запуск проверок качества кода (lint, typecheck, tests)
</technologies_used>

<implementation_details>
**Особенности реализации:**

### Версионирование через .cursor/cursor-rules-config.json

Создается в директории `.cursor/` при инициализации. Содержит расширенную конфигурацию правил:

- **configVersion** — версия формата конфигурации (semver)
- **version** — версия установленных правил
- **installedAt** — дата установки (ISO 8601)
- **updatedAt** — дата последнего обновления (ISO 8601)
- **source** — источник правил (имя пакета)
- **settings.language** — язык вывода сообщений (`ru` или `en`)
- **ruleSets** — наборы правил с флагом обновления (`update: boolean`) и фиксированной версией (опционально)
- **ignoreList** — список файлов для игнорирования с поддержкой glob-паттернов
- **fileOverrides** — переопределения YAML frontmatter для конкретных файлов

**Причина:** Обеспечивает отслеживание версий без зависимости от git или других VCS, позволяет гибко управлять обновлениями и настройками правил.

### Работа с YAML frontmatter через gray-matter

Используется библиотека `gray-matter` для парсинга и модификации YAML frontmatter в файлах правил. Позволяет применять переопределения параметров через `fileOverrides` в конфигурации.

**Применение:** Функция `applyYamlOverrides` объединяет существующие YAML параметры с переопределениями из конфигурации (merge-стратегия).

### Фильтрация файлов через micromatch

Используется библиотека `micromatch` для проверки файлов по списку игнорирования с поддержкой glob-паттернов (`**/*.mdc`, `rules/custom-*.mdc` и т.д.).

**Применение:** Функция `shouldIgnoreFile` нормализует пути (кроссплатформенная совместимость) и проверяет соответствие файла паттернам из `ignoreList`.

### ESM модули с node: префиксом

Все Node.js импорты используют `node:` префикс (node:fs/promises, node:path). Полная поддержка ESM без CommonJS. Используется нативный `node:fs/promises` вместо fs-extra.

**Преимущества:**

- Нативный `node:fs/promises` устраняет зависимость от fs-extra и CommonJS/ESM конфликты
- `picocolors` обеспечивает безопасность (chalk 5.6.1 был скомпрометирован в сентябре 2025) и производительность (2x быстрее, 14x легче)
- `citty` предоставляет современный типобезопасный API для CLI с поддержкой async commands
- `gray-matter` обеспечивает надежный парсинг YAML frontmatter
- `micromatch` предоставляет быструю и точную работу с glob-паттернами

### Автоматическая проверка версии

Перед выполнением любой CLI команды автоматически проверяется наличие новой версии пакета в npm registry. При обнаружении обновления пакет автоматически обновляется (через npm/yarn) и процесс перезапускается для загрузки нового кода.

**Реализация:** Функция `ensureLatestVersion` вызывается в `runCli` перед запуском команд. После успешного обновления вызывается `respawnProcess()`, который запускает новый процесс через `child_process.spawn()` с detached флагом и завершает текущий процесс. Это гарантирует выполнение обновленного кода вместо закешированного в памяти старого. Ошибки проверки версии не блокируют выполнение команд (только предупреждение в консоль).

**Process respawn механизм:** После установки новой версии через `npm install -g`, текущий Node.js процесс продолжал бы выполнять старый код из памяти. Функция `respawnProcess()` решает эту проблему перезапуском процесса с теми же аргументами командной строки, обеспечивая загрузку свежего кода с диска.

### Важные детали архитектуры:

- Все модули следуют facade pattern с index.ts
- Named exports only (запрет default exports)
- Один файл = одна функция (кроме helpers.ts)
- Тесты в `__tests__/` рядом с исходниками
- Внутренние функции не экспортируются из фасадов (приватная реализация)
</implementation_details>

<development_commands>
**⚠️ ДЛЯ ИИ АГЕНТОВ:**

Перед финальной проверкой качества кода **ВСЕГДА** проверяйте наличие унифицированной команды в `package.json` (например, `yarn lint` или `npm run lint`). Используйте именно эту команду вместо отдельных команд типа `yarn lint:test-unit`, `yarn lint:eslint` и т.д.

**Примеры неправильного использования для ИИ:**

- ❌ `yarn lint:test-unit` — пропускает другие проверки
- ❌ `yarn lint:eslint` — пропускает тесты и typecheck
- ❌ `yarn lint:ts` — пропускает тесты и линтер
- ✅ `yarn lint` — запускает ВСЕ проверки параллельно

**КРИТИЧЕСКИ ВАЖНО: ЕДИНАЯ КОМАНДА ДЛЯ ВСЕХ ПРОВЕРОК**

🚨 **ЗАПРЕЩЕНО использовать отдельные команды для тестов, линтеров и typecheck!**

**ОБЯЗАТЕЛЬНО использовать ЕДИНУЮ команду:**

```bash
# ✅ ЕДИНСТВЕННЫЙ ПРАВИЛЬНЫЙ СПОСОБ - используй ТОЛЬКО эту команду!
yarn lint
# или
npm run lint
```

**Что делает команда `lint`:**

Команда `lint` запускает **ВСЕ** проверки качества кода параллельно через `ai-friendly-runner`:

- ✅ **ESLint** (`lint:eslint`) — проверка стиля кода и исправление ошибок
- ✅ **TypeScript** (`lint:ts`) — проверка типов (`tsc --noEmit`)
- ✅ **Knip** (`lint:knip`) — проверка неиспользуемого кода и экспортов
- ✅ **Unit тесты** (`lint:test-unit`) — запуск всех unit тестов через Vitest
- ✅ **E2E тесты** (`lint:test-e2e`) — запуск всех E2E тестов через Vitest

**КРИТИЧЕСКИЕ ПРАВИЛА:**

1. **ЗАПРЕЩЕНО** запускать отдельные команды (`lint:eslint`, `lint:ts`, `lint:test-unit` и т.д.)
2. **ОБЯЗАТЕЛЬНО** использовать только `yarn lint` или `npm run lint`
3. **ОБЯЗАТЕЛЬНО** запускать перед каждым коммитом
4. **ОБЯЗАТЕЛЬНО** убедиться что все проверки прошли успешно (exit code = 0)

**Почему единая команда:**

- ⚡ Параллельное выполнение всех проверок (экономия времени в 2-3 раза)
- 📊 Единый отчет с цветным выводом (✅/❌) для всех проверок
- 🔍 Автоматический exit code (0 = успех, 1 = ошибка)
- 🎯 Детальные ошибки только для упавших команд (минимум шума)
- 🔒 Гарантия что все проверки выполнены (нельзя забыть про тесты или typecheck)

**Дополнительные команды (только для специфических случаев):**

```bash
# 📦 СБОРКА - проверка готовности к production
npm run build
# Использование: только для проверки сборки перед релизом

# 🔥 РАЗРАБОТКА - запуск CLI в режиме разработки
npm run start
# Использование: для тестирования CLI команд локально
```

**⚠️ ПОСЛЕДСТВИЯ НАРУШЕНИЯ ПРАВИЛ:**

- Использование отдельных команд → пропуск проверок → баги в production
- Игнорирование `lint` → накопление технического долга → замедление разработки
- Пропуск проверок → потеря доверия команды → проблемы с качеством кода

**✅ ПРОФЕССИОНАЛЬНЫЙ СТАНДАРТ:**

Выполнение `yarn lint` перед каждым коммитом = статус Senior Developer = уважение команды = качественный код!
</development_commands>
